{
  "name": "jspulse",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  }
} 