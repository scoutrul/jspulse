{
  "name": "jspulse-backend",
  "version": "0.1.0",
  "description": "Backend для JS Пульс - агрегатора вакансий по фронтенду",
  "type": "module",
  "main": "./dist/backend/src/index.js",
  "scripts": {
    "start": "node dist/backend/src/index.js",
    "dev": "nodemon",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "seed:run": "node -r dotenv/config dist/backend/src/data/seedDatabase.js",
    "fetch:hh:run": "node -r dotenv/config dist/backend/src/scripts/fetchAndSaveFromHH.js",
    "clear:db:run": "node -r dotenv/config dist/backend/src/scripts/clearDatabase.js",
    "db:refresh": "pnpm run build && pnpm run clear:db:run && pnpm run seed:run && pnpm run fetch:hh:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@jspulse/shared": "workspace:*",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "ky": "^1.8.1",
    "mongoose": "^8.13.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
