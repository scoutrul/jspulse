{
  "name": "jspulse-backend",
  "version": "0.1.0",
  "description": "Backend для JS Пульс - агрегатора вакансий по фронтенду",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon --watch './src/**/*.ts' --watch './*.ts' --ext ts,json --exec 'pnpm run build && node dist/index.js'",
    "build": "rm -rf dist && tsc -p tsconfig.json --listEmittedFiles",
    "seed:run": "node dist/data/seedDatabase.js",
    "fetch:hh:run": "node dist/scripts/fetchAndSaveFromHH.js",
    "clear:db:run": "node dist/scripts/clearDatabase.js",
    "db:refresh": "pnpm run build && pnpm run clear:db:run && pnpm run seed:run && pnpm run fetch:hh:run",
    "seed": "node dist/data/seedDatabase.js",
    "fetch:hh": "node dist/scripts/fetchAndSaveFromHH.js",
    "clear:db": "node dist/scripts/clearDatabase.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "@jspulse/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
