# Общие настройки
NODE_ENV=development

# MongoDB (для локального запуска вне Docker)
# Используется в backend/.env для Docker-окружения
MONGO_URL=mongodb://localhost:27017/jspulse

# Backend
PORT=3001
API_URL=http://localhost:3001

# Frontend (переменные с префиксом VITE_ лучше определять в frontend/.env)
# VITE_API_URL=http://localhost:3001

# Добавьте свои значения и переименуйте файл в .env
